import React from "react";
import "../../styles/globals.css";
const SandboxLayout: React.FC<{ children: React.ReactNode }> = ({
  children,
}) => {
  return <>{children}</>;
};

export default SandboxLayout;
